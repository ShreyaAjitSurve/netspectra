# netspectra
NetSpectra is a real-time analyzer that transforms raw packets into visual intelligence. It classifies TCP, UDP, ICMP, DNS, and ARP traffic while tracking live throughput. Features include a "Top Talkers" IP leaderboard, a live packet feed, and dual-layer analytics that distinguish between packet frequency and cumulative bandwidth volume.
